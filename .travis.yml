language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ng-zhaimi' ]; then cd .. && eval "mv $currentfolder generator-ng-zhaimi" && cd generator-ng-zhaimi; fi

